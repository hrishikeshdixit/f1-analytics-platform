version: 2

sources:
  - name: raw
    database: f1-analytics-491120
    schema: raw
    tables:
      - name: laps
        description: "Raw lap data loaded from FastF1"
  
      - name: telemetry
        description: "Raw telemetry data — all laps, all sessions, partitioned by ingestion date, clustered by Driver/SessionType/RoundNumber"